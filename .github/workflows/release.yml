name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
        
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Important: gets full git history including tags
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Determine release type
      run: |
        if [[ "${{ github.ref_name }}" == *"-"* ]]; then
          echo "🔄 Creating PRE-RELEASE for tag: ${{ github.ref_name }}"
          echo "EP_PRE_RELEASE=true" >> $GITHUB_ENV
        else
          echo "🚀 Creating PRODUCTION RELEASE for tag: ${{ github.ref_name }}"
          echo "EP_PRE_RELEASE=false" >> $GITHUB_ENV
        fi
      shell: bash
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build application
      run: npm run build
      
    - name: Build and publish to GitHub Releases
      run: |
        echo "🔧 Building and publishing with electron-builder..."
        echo "Tag: ${{ github.ref_name }}"
        echo "Pre-release: ${EP_PRE_RELEASE}"
        npm run publish -- --publish=always
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        DEBUG: electron-builder
        
    - name: Create GitHub Release (fallback)
      if: always()
      uses: softprops/action-gh-release@v2
      with:
        files: release/*
        prerelease: ${{ contains(github.ref_name, '-') }}
        generate_release_notes: true
        name: Release ${{ github.ref_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-${{ matrix.os }}
        path: release/
        retention-days: 7
